* Fix a bug for unknown reason on non-Linux Unix systems
* Update parser for coru
  * Detect line wrapping
  * Detect string
  * Detect escape character in string
* Test whether modified code are still valid
* Add parser for uncoru
* Add `corucb`, aka `coru` for clipboard
* Add `uncorucb`, aka `uncoru` for clipboard
